Inspect the uploaded ZIP directly. Read START-HERE-ASTRA.md and approved-job.json first. Treat the human-approved floor-plan set and approved orientation screenshots/direction labels as authoritative inputs. Do not redo broad source discovery or silently replace the Primary source. CRITICAL ORIENTATION FAST PATH: when an approved orientation source already establishes flat directions/north, preserve its handedness; do NOT infer/search/optimize orientation from footprint geometry, do NOT mirror/reflect the plan, and do NOT enumerate 0/90/180/270 or other alternative rotations. Apply only the single geospatial transform consistent with the approved source. Geometry-fit error must never override an approved flat direction. If approved semantics conflict with LandsD/CSDI geometry, write the tower to needs-review.json instead of trying another orientation. Use official LandsD/CSDI geometry for precise georeferencing, run QA/tests, import only passing results, and record per-tower/per-stage elapsed timings in processing-timings.json plus processing.log so bottlenecks are visible.
